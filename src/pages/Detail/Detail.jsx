import React from 'react';

const Detail = () => {
  return (
    <main>
      <button className='button' onClick={''}>
        Back
      </button>
      Detail working
    </main>
  );
};

export default Detail;
